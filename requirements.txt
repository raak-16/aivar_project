yfinance>=0.2.54
pandas>=2.0.0
python-dotenv>=1.0.0
Flask>=3.0.0
Flask-SocketIO>=5.3.0

# TA-Lib for technical analysis
TA-Lib>=0.4.26

# VectorBT for backtesting
vectorbt>=0.21.0

# Required when enabling the bundled Kronos financial forecasting model.
torch>=2.0.0
huggingface_hub>=0.33.1
einops==0.8.1
safetensors==0.6.2
tqdm>=4.67.1

# Use the local multi-agent financial analysis engine (now vendored in this repo).
-e ./tradingagents
